# P9 · S11 — Gate sonde + twist→angle plume + hover atténué   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 5 (parallèle : non — CONDITIONNELLE au gate T-138)**
> Exécutant : UNIQUEMENT les tâches ci-dessous. Design fixé — ne reconçois pas.
> **NE PAS LANCER tant que T-138 (validation humaine de la sonde) n'a pas conclu « go ».**
> Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-03 · Branche : — · Dépend de : S3 (sonde), T-138 (gate humain)

## Lire (commun à la session)
- `DECISIONS.md §D-012` pt 8 (Pencil Pro conditionnel).
- Résultats de la sonde (T-137/S3) validés par Thibault (quels champs varient sur l'iPad réel).
- `src/app.js` : `pointerPressure` l.1429-1431, `moveStroke` l.1447-1466, `applyStroke` l.1307-1325,
  `edit.calliAngle` (état l.905, câblage `#calli-angle` l.1828-1831), `buildEditCursor` l.1658-1700
  (nib incliné en mode calli), `updateEditCursor`/`hideEditCursor` l.1643-1654.
- `test/pencil-probe.html` (référence des noms de champs).

## Hors périmètre
- **Ne rien coder si T-138 = no-go** : dans ce cas, laisser S11/S12 à l'état `[ ]` et conserver le slider
  `#calli-angle` comme seul contrôle d'angle. Signaler et rendre la main.
- Aucune géométrie nouvelle, aucun `vendor/`.

---

## T-138 — [Gate humain] Valider les capacités Pencil Pro sur iPad

### Objectif
Décider go/no-go du Lot E d'après la sonde étendue, sur matériel réel.

### Décision clé
`DECISIONS.md §D-012` pt 8. Critère go pour T-139 : `twist` **varie** réellement quand Thibault roule le
Pencil Pro (sinon barrel roll non exposé → no-go T-139, le slider reste). Critère go pour T-140 :
`altitudeAngle` varie à l'inclinaison.

### Lire / Modifier
- Aucune modification de code (tâche humaine). L'exécutant IA **n'exécute pas** cette tâche : elle est
  tranchée par Thibault et consignée dans `VALIDATION.md` (P9 · S11 · gate).

### Étapes
1. (Humain) Ouvrir `test/pencil-probe.html` sur iPad + Pencil Pro. Rouler / incliner le stylet.
2. (Humain) Noter dans `VALIDATION.md` : `twist` varie oui/non ; `altitudeAngle` varie oui/non.
3. (Humain) Conclure go/no-go pour T-139 (twist) et T-140 (tilt).

### Validation
- Humain : verdict consigné dans `VALIDATION.md`. → conditionne la suite de S11 et S12.

### Si bloqué
Sonde non concluante (champs ambigus) → rester en no-go (fallback slider), réévaluer plus tard.

### Statut
[ ] à faire (humain) · exécuté le : — · commit : —

---

## T-139 — twist → angle de plume + hover atténué   (si go)

### Objectif
Piloter l'angle de la plume calligraphique par la rotation physique du fût (Pencil Pro), le slider
devenant fallback ; atténuer le curseur en survol pur.

### Décision clé
`DECISIONS.md §D-012` pt 8. `twist` (0-359°) → `edit.calliAngle` en **mode Plume actif uniquement** ;
le slider reste maître si `twist` figé/non exposé (déjà tranché par T-138). Hover : quand `buttons===0`
(survol Pencil), curseur en opacité réduite pour distinguer « je vise » de « je touche ».

### Lire / Modifier
- Modifier : `src/app.js`.

### Étapes
1. Dans `moveStroke`/`editPointerMove`, en mode `strokeMode === "calli"` et `e.pointerType === "pen"`,
   si `twist` est exploitable, mettre à jour `edit.calliAngle = e.twist` (mapping/orientation à caler
   pour que le nib suive intuitivement le fût) et rafraîchir `#calli-angle` (slider) + `buildEditCursor`.
   Débrancher proprement si le stylet n'expose pas `twist` (garde `Number.isFinite` + valeur qui bouge).
2. Ne pas parasiter les autres modes : n'agir qu'en Plume.
3. **Hover atténué** : dans `updateEditCursor` (l.1648-1654), si l'événement est un survol
   (`e.buttons === 0` et `pointerType === "pen"`), appliquer une opacité réduite au `editCursorNode` ;
   opacité pleine dès qu'on trace. (Le hover fonctionne déjà par construction — c'est un raffinement.)

### Validation
- Auto : `node test/run.js` → vert.
- Humain : sur iPad + Pencil Pro — en mode Plume, rouler le stylet fait tourner le nib/l'angle du trait ;
  le slider reste utilisable et cohérent ; en survol, le curseur est visiblement atténué. → `VALIDATION.md` (P9 · S11).

### Si bloqué
Si le mapping `twist`→angle est instable/inversé au point d'être inutilisable → STOP, signale ; repli :
laisser le slider seul (no-op sur twist) sans régresser l'existant.

### Commit
`feat(edit): angle de plume piloté par le twist du Pencil Pro + hover atténué [P9/T-139]`

### Statut
[ ] à faire (si go T-138) · exécuté le : — · commit : —

---

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : statut/visuel dans ce fichier). Si go tilt, **S12** suit.
