# P11 · S6 — Consolidation : bancs d'essai, statuts, commits, push   (rédigé par Opus)

> **Modèle : Haiku · effort : low**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT la tâche ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-03 · Branche : —

## Lire (commun à la session)

- `plans/P11/index.md` et `S1.md`-`S5.md` (statut de chaque tâche, message de commit prévu par
  tâche).
- `STATUS.md`, `TASKS.md`, `VALIDATION.md` (format et plafonds de lignes en vigueur, cf.
  `Templates/WORKFLOW.md` §7 — 80/60/120 lignes respectivement).

## Hors périmètre

- Ne modifie AUCUN fichier de code (`src/`, `tools/`, `reference/prompts-motifs.html`) — cette
  session ne fait que valider, documenter et committer le travail déjà fait par S1-S5.
- Ne lance PAS la génération d'images ni le pipeline `motif-axes.py`/`build-motif-bank.py` sur les
  prompts modifiés en S5 — ça reste un aller-retour humain hors plan (cf. note en tête de `S5.md`).

---

## T8 — Bancs d'essai, mise à jour des fichiers de suivi, commits, push

### Objectif

Vérifier que l'ensemble S1-S5 est vert, mettre à jour les fichiers de contexte, committer tâche par
tâche (pas de commit global), pousser une seule fois.

### Décision clé

Suivre exactement `Templates/WORKFLOW.md` §4b : jamais `git add -A`/`git commit -a` ; un commit par
tâche avec staging explicite des fichiers listés dans chaque `S<k>.md` ; un seul `git push` à la fin.

### Lire / Modifier

En plus du commun : `STATUS.md`, `TASKS.md`, `VALIDATION.md` (mise à jour) ; `plans/P11/index.md`
(cases `[x]`).

### Étapes

1. Exécuter, dans l'ordre, et vérifier qu'ils sont tous verts :
   - `node test/run.js`
   - `node test/branch-proto-check.js`
   - `node test/banc-poses.js`
   - `node test/banc-composants.js` (comparer sa sortie à celle du cadrage : les 3 skews et les 2
     corps vides doivent toujours apparaître tant que les images sources n'ont pas été régénérées
     par Thibault — normal, ce n'est PAS une régression de S1-S5, c'est un rappel que S5·T7 n'a fait
     que les signaler, pas les corriger).
   - Le script ad hoc de `S1.md` (T1, étape 5) et toute assertion ajoutée par `S2.md` (T2, étape 3).
2. Si un des bancs échoue de façon inattendue (pas les 5 anomalies déjà connues de
   `banc-composants.js`) → STOP, ne pas committer, signaler la régression à Thibault avec la sortie
   complète du banc concerné.
3. Mettre à jour `STATUS.md` : une entrée courte décrivant ce que P11 a changé (jonction organique→
   piste, fusion radicelle, pastille montée, espacement composants, seuils `motif-axes.py`, prompts
   kodama/korok/composants) — dans le respect du plafond de 80 lignes (purger si dépassement, cf.
   `/purge-contexte`).
4. Mettre à jour `TASKS.md` : remplacer les entrées correspondantes (ou en ajouter si absentes) par
   des renvois `→ plans/P11/S<k>.md`, respecter le plafond de 60 lignes.
5. Ajouter à `VALIDATION.md` les points N2 restés à juger par Thibault (transition organique→piste,
   fusion radicelle, pastille de connexion, espacement, seuils champignon) — respecter le plafond de
   120 lignes.
6. Committer **tâche par tâche**, staging explicite (`git add <fichiers listés dans le S<k>.md
   correspondant>`), message = celui déjà écrit dans chaque `S<k>.md` (« Message de commit »).
7. Cocher les cases `[x]` de `plans/P11/index.md` au fur et à mesure.
8. Un seul `git push` une fois tous les commits faits.

### Validation

- **N0 auto (bloque le commit)** : tous les bancs de l'étape 1 verts (à l'exception documentée de
  `banc-composants.js`, cf. étape 1).
- **N1 visuel auto** : — (session de consolidation, aucune UI touchée).
- **N2 humain** : — (déjà couvert par les cases N2 de S1-S5 ; cette session ne fait que reporter
  les points restants dans `VALIDATION.md`, pas les juger elle-même).

### Si bloqué

Tout échec de banc non attendu, ou tout fichier de suivi qui dépasserait son plafond après mise à
jour sans qu'une purge simple règle le problème → STOP, signaler plutôt que de committer un état
incomplet ou un fichier hors plafond.

### Message de commit (appliqué en fin de plan)

Un commit par tâche (cf. messages listés dans `S1.md`-`S5.md`), puis un commit final dédié :
`docs: consolidation P11 — STATUS/TASKS/VALIDATION à jour`

---

## Fin de session

Dérouler `/fin-de-tache` (mode solo — session finale de consolidation, hors vague parallèle).
