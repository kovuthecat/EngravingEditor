# P7 · T1 — Garde `module.exports` en fin de `vendor/clipper.js` (= T-106)   (rédigé par Opus)

> Exécutant : fais UNIQUEMENT cette tâche, dans l'ordre des étapes. Lis UNIQUEMENT les fichiers
> sous « Lire ». Ne crée aucun fichier/dépendance hors « Modifier ». Design fixé — ne reconçois pas.
> Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-02 · Modèle : Haiku · effort : minimal · Branche : —

## Objectif
Supprimer la `ReferenceError: module is not defined` levée en console navigateur au chargement :
la **dernière ligne** de `vendor/clipper.js` est un `module.exports = ClipperLib;` non gardé.

## Décision clé
`DECISIONS.md §D-010` (volet 3) : exception vendored **d'une seule ligne** — reproduire exactement la
garde `typeof module` déjà présente aux l. 81-83 du même fichier. L'erreur est bénigne (dernière
instruction du fichier, `ClipperLib` déjà global) mais pollue la console. Node (tests) garde son export.

## Lire
- `vendor/clipper.js` : l. 78-85 (la garde existante, modèle à copier) et l. 6980-6986 (la ligne fautive).

## Modifier
- `vendor/clipper.js` (la dernière ligne uniquement).

## Hors périmètre
- Aucune autre modification de `vendor/` (règle générale maintenue ; D-010 n'autorise que cette ligne).
- Ne pas supprimer l'export : `test/run.js` tourne sous Node et peut en dépendre.

## Étapes
1. Remplacer la dernière ligne `module.exports = ClipperLib;` par :
   `if (typeof module !== 'undefined' && module.exports) module.exports = ClipperLib;`
2. Vérifier qu'il ne reste aucun autre `module.` non gardé : `grep -n "module" vendor/clipper.js`
   → seules la garde l. 81-83 et la ligne modifiée doivent apparaître.

## Validation
- Auto (bloque le commit) : `node test/run.js` → vert (l'export Node fonctionne toujours).
- Humain (visuel/UX, non bloquant) : ouvrir l'app, console navigateur **sans** `module is not defined`
  au chargement → reporter dans `VALIDATION.md`.

## Si bloqué
Si `node test/run.js` échoue après la modification (chargement de ClipperLib côté Node cassé) → STOP,
restaurer la ligne d'origine et signaler.

## Commit
`fix(vendor): garde l'export CommonJS final de clipper.js (erreur console navigateur)`

## Statut
[ ] à faire · exécuté par : — · le : — · commit : —
